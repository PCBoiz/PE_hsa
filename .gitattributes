# Kịch bản bash của cổng pre-push phải giữ LF kể cả khi core.autocrlf=true (máy Windows):
# bash của Git for Windows đọc CR như một phần câu lệnh ("$'\r': command not found").
.githooks/** text eol=lf
