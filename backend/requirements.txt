Django>=5.2,<5.3
djangorestframework>=3.15
djangorestframework-simplejwt[crypto]>=5.4
django-cors-headers>=4.4
django-allauth[socialaccount]>=65.0
dj-database-url>=2.2
psycopg[binary,pool]>=3.2
python-dotenv>=1.0
email-validator>=2.1
requests>=2.32
gunicorn>=21.2
whitenoise>=6.7
# Chatbot Trợ lý HSA (LangGraph + DeepSeek qua API OpenAI-compatible)
langgraph>=1.2.10
langchain-openai>=1.4.2
# Bộ đệm dùng chung cho 2 worker gunicorn — chỉ được dùng khi có biến REDIS_URL
# (xem chú thích ở config/settings.py). Không có biến thì gói này nằm im.
django-redis==5.4.0
# Đọc/ghi bảng tính .xlsx cho đường NHẬP nội dung (đề thi thử, giáo trình).
# Thuần Python, không phần mở rộng C — cài trên Render không cần trình biên dịch.
# Vì sao .xlsx chứ không chỉ .csv: Excel bản tiếng Việt lưu CSV theo bảng mã hệ
# thống, nên "Định lượng" quay về thành ký tự hỏng. Ô .xlsx là chuỗi Unicode.
openpyxl>=3.1
